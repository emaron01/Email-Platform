/**
 * Suggested referral share text — reps may rewrite before copying.
 */
export function referralShareMessage(code: string): string {
  return (
    `I've been using Aimed Outreach to research prospects and write outbound emails — it saves ` +
    `me the half hour per account I never actually had. Use code ${code} when you sign up and ` +
    `you'll get 10% off for as long as you use it. aimedoutreach.com`
  );
}
